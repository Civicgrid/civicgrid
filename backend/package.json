{
  "name": "civicgrid-backend",
  "version": "0.1.0",
  "private": true,
  "description": "CivicGrid oracle services — KYC credential issuance and IoT meter simulation",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.3.0",
    "express": "^4.21.2",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2",
    "ts-node-dev": "^2.0.0"
  }
}
